<?php
declare(strict_types=1);
$a=$_GET['a']??'products';
if(!in_array($a,['products','new_product','edit_product','save_product','update_product'],true)){
 require __DIR__.'/module_costs.php';
 exit;
}
session_start();
$root=dirname(__DIR__,3);$cfg=require $root.'/config.php';date_default_timezone_set($cfg['timezone']??'America/Argentina/Buenos_Aires');
$db=new PDO('mysql:host='.$cfg['db_host'].';dbname='.$cfg['db_name'].';charset=utf8mb4',$cfg['db_user'],$cfg['db_pass'],[PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION,PDO::ATTR_DEFAULT_FETCH_MODE=>PDO::FETCH_ASSOC]);
if(empty($_SESSION['user'])){header('Location:index.php');exit;}
$_SESSION['csrf']??=bin2hex(random_bytes(24));
require_once $root.'/app/Core/UnifiedSidebar.php';
function e2($v):string{return htmlspecialchars((string)$v,ENT_QUOTES,'UTF-8');}
function go2(string $u):never{header('Location:'.$u);exit;}
function csrf2():void{if(!hash_equals($_SESSION['csrf']??'',$_POST['csrf']??'')){http_response_code(419);exit('Solicitud vencida.');}}
function canManage2():bool{if(($_SESSION['user']['role']??'')==='admin')return true;return !empty($_SESSION['user']['permissions']['products']['manage']);}
function usd2($n):string{return 'US$ '.number_format((float)$n,2,',','.');}
function sale2(float $cost,float $markup):float{return round($cost*(1+$markup/100),2);}
function hasOverridesTable(PDO $db):bool{try{$db->query('SELECT 1 FROM product_price_overrides LIMIT 1');return true;}catch(Throwable $e){return false;}}
function pageTop(string $title,string $current='products'):void{$msg=$_SESSION['msg']??null;unset($_SESSION['msg']);?><!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title><?=e2($title)?> · Punto ERP</title><style><?=erpSidebarCss()?>body{margin:0;background:var(--bg);color:var(--ink);font:15px/1.45 system-ui}.main{margin-left:var(--sidebar);padding:32px 4%}.card{background:#fff;border:1px solid var(--line);border-radius:14px;padding:22px;margin-bottom:18px}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:16px}.c3{grid-column:span 3}.c4{grid-column:span 4}.c6{grid-column:span 6}.c8{grid-column:span 8}.c12{grid-column:span 12}label{display:block;font-weight:650;margin-bottom:6px}input,select{width:100%;padding:10px 11px;border:1px solid #cbd1d7;border-radius:8px;font:inherit;background:#fff}input[type=checkbox]{width:auto}.btn{display:inline-block;border:0;border-radius:8px;padding:10px 15px;background:var(--o);color:#fff;text-decoration:none;font-weight:700;cursor:pointer}.btn.light{background:#edf0f3;color:var(--ink)}.actions{display:flex;gap:9px;align-items:center;flex-wrap:wrap}.muted{color:var(--muted)}.flash{padding:12px 16px;background:#fff0e5;border:1px solid #ffd2b1;border-radius:9px;margin-bottom:18px}.scroll{overflow:auto}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:11px 9px;border-bottom:1px solid var(--line);vertical-align:middle}th{font-size:12px;text-transform:uppercase;color:var(--muted)}.right{text-align:right}.pill{display:inline-block;padding:4px 9px;border-radius:20px;background:#edf0f3;font-size:12px}.price-box{background:#fafbfc;border:1px solid var(--line);border-radius:12px;padding:14px}.price-value{font-weight:800;font-size:17px}.special{color:#ff6702;font-weight:700}@media(max-width:900px){.main{margin-left:0;padding:22px}.c3,.c4,.c6,.c8{grid-column:span 12}}</style></head><body><?php erpSidebar($current);?><main class="main"><?php if($msg):?><div class="flash"><?=e2($msg)?></div><?php endif;?><?php }
function pageBottom():void{?></main></body></html><?php }
$hasOverrides=hasOverridesTable($db);
if(in_array($a,['save_product','update_product'],true)){
 if(!canManage2()){http_response_code(403);exit('No autorizado.');}csrf2();
 if(!$hasOverrides){$_SESSION['msg']='Primero ejecutá la migración 2026_09_07_product_price_overrides.sql.';go2('index.php?a='.($a==='update_product'?'edit_product&id='.(int)($_POST['product_id']??0):'new_product'));}
 try{
  $id=(int)($_POST['product_id']??0);$sku=trim((string)($_POST['sku']??''));$description=trim((string)($_POST['description']??''));$category=(int)($_POST['category_id']??0)?:null;$unit=trim((string)($_POST['unit']??'Unidad'))?:'Unidad';$cost=max(0,(float)($_POST['cost_usd']??0));$track=!empty($_POST['track_stock'])?1:0;$active=!empty($_POST['active'])?1:0;$initial=max(0,(float)($_POST['initial_stock']??0));
  if($sku===''||$description==='')throw new Exception('Completá SKU y descripción.');
  $db->beginTransaction();
  if($a==='save_product'){$db->prepare('INSERT INTO products(sku,description,category_id,unit,cost_usd,track_stock,stock_quantity,active) VALUES(?,?,?,?,?,?,?,?)')->execute([$sku,$description,$category,$unit,$cost,$track,$track?$initial:0,$active]);$id=(int)$db->lastInsertId();if($track&&$initial>0)$db->prepare("INSERT INTO stock_movements(product_id,movement_date,movement_type,quantity,reference,notes,user_id) VALUES(?,CURDATE(),'inicial',?,'Alta del producto','Stock inicial',?)")->execute([$id,$initial,$_SESSION['user']['id']??null]);}
  else{$db->prepare('UPDATE products SET sku=?,description=?,category_id=?,unit=?,cost_usd=?,track_stock=?,active=? WHERE id=?')->execute([$sku,$description,$category,$unit,$cost,$track,$active,$id]);}
  $lists=$db->query('SELECT id,markup_percentage FROM price_lists')->fetchAll();$posted=$_POST['list_markup']??[];
  $up=$db->prepare('INSERT INTO product_price_overrides(product_id,price_list_id,markup_percentage) VALUES(?,?,?) ON DUPLICATE KEY UPDATE markup_percentage=VALUES(markup_percentage)');$del=$db->prepare('DELETE FROM product_price_overrides WHERE product_id=? AND price_list_id=?');
  foreach($lists as $l){$lid=(int)$l['id'];$def=(float)$l['markup_percentage'];$raw=$posted[$lid]??'';$value=$raw===''?$def:(float)$raw;if($value<0)throw new Exception('El porcentaje no puede ser negativo.');if(abs($value-$def)<0.0005)$del->execute([$id,$lid]);else$up->execute([$id,$lid,$value]);}
  $db->commit();$_SESSION['msg']='Producto guardado. Los porcentajes especiales quedaron aplicados solo a este producto.';go2('index.php?a=edit_product&id='.$id);
 }catch(Throwable $e){if($db->inTransaction())$db->rollBack();$_SESSION['msg']='No se pudo guardar: '.$e->getMessage();go2($_SERVER['HTTP_REFERER']??'index.php?a=products');}
}
if($a==='products'){
 $q=trim((string)($_GET['q']??''));$cat=(int)($_GET['category_id']??0);$listId=(int)($_GET['price_list_id']??0);$lists=$db->query('SELECT * FROM price_lists WHERE active=1 ORDER BY id')->fetchAll();if(!$listId&&$lists)$listId=(int)$lists[0]['id'];$selected=null;foreach($lists as $l)if((int)$l['id']===$listId)$selected=$l;$cats=$db->query('SELECT * FROM product_categories WHERE active=1 ORDER BY name')->fetchAll();$where=['1=1'];$params=[];if($q!==''){$where[]='(p.sku LIKE ? OR p.description LIKE ?)';$params[]="%$q%";$params[]="%$q%";}if($cat){$where[]='p.category_id=?';$params[]=$cat;}
 $join=$hasOverrides&&$selected?' LEFT JOIN product_price_overrides ppo ON ppo.product_id=p.id AND ppo.price_list_id='.(int)$selected['id']:'';$s=$db->prepare("SELECT p.*,pc.name category_name".($hasOverrides&&$selected?',ppo.markup_percentage override_markup':'')." FROM products p LEFT JOIN product_categories pc ON pc.id=p.category_id $join WHERE ".implode(' AND ',$where)." ORDER BY p.description");$s->execute($params);$rows=$s->fetchAll();pageTop('Productos');?><div class="actions"><div style="flex:1"><h1>Productos</h1><p class="muted">El precio usa el porcentaje general de la lista, salvo que el producto tenga un porcentaje especial.</p></div><?php if(canManage2()):?><a class="btn light" href="?a=price_lists">Listas de precios</a><a class="btn" href="?a=new_product">+ Producto</a><?php endif;?></div><div class="card"><form method="get"><input type="hidden" name="a" value="products"><div class="grid"><p class="c4"><label>Buscar</label><input name="q" value="<?=e2($q)?>"></p><p class="c4"><label>Categoría</label><select name="category_id"><option value="0">Todas</option><?php foreach($cats as $c):?><option value="<?=$c['id']?>" <?=$cat==(int)$c['id']?'selected':''?>><?=e2($c['name'])?></option><?php endforeach;?></select></p><p class="c4"><label>Lista</label><select name="price_list_id"><?php foreach($lists as $l):?><option value="<?=$l['id']?>" <?=$listId==(int)$l['id']?'selected':''?>><?=e2($l['name'])?> (+<?=e2($l['markup_percentage'])?>%)</option><?php endforeach;?></select></p></div><button class="btn light">Aplicar</button></form></div><div class="card scroll"><table><tr><th>SKU</th><th>Descripción</th><th>Costo</th><th>Lista</th><th>% efectivo</th><th class="right">Precio venta</th><th>Acciones</th></tr><?php foreach($rows as $r):$markup=$selected?(isset($r['override_markup'])&&$r['override_markup']!==null?(float)$r['override_markup']:(float)$selected['markup_percentage']):0;$special=$selected&&isset($r['override_markup'])&&$r['override_markup']!==null;?><tr><td><b><?=e2($r['sku'])?></b></td><td><?=e2($r['description'])?></td><td><?=usd2($r['cost_usd'])?></td><td><?=e2($selected['name']??'—')?></td><td><?=$selected?e2($markup).'%'.($special?' <span class="special">Especial</span>':''): '—'?></td><td class="right"><b><?=$selected?usd2(sale2((float)$r['cost_usd'],$markup)):'—'?></b></td><td><a class="btn light" href="?a=edit_product&id=<?=$r['id']?>">Editar</a></td></tr><?php endforeach;?></table></div><?php pageBottom();exit;
}
if(in_array($a,['new_product','edit_product'],true)){
 $p=['id'=>0,'sku'=>'','description'=>'','category_id'=>null,'unit'=>'Unidad','cost_usd'=>0,'track_stock'=>1,'stock_quantity'=>0,'active'=>1];if($a==='edit_product'){$s=$db->prepare('SELECT * FROM products WHERE id=?');$s->execute([(int)($_GET['id']??0)]);$p=$s->fetch();if(!$p)exit('Producto inexistente.');}
 $cats=$db->query('SELECT * FROM product_categories WHERE active=1 ORDER BY name')->fetchAll();$lists=$db->query('SELECT * FROM price_lists WHERE active=1 ORDER BY id')->fetchAll();$overrides=[];if($p['id']&&$hasOverrides){$s=$db->prepare('SELECT price_list_id,markup_percentage FROM product_price_overrides WHERE product_id=?');$s->execute([$p['id']]);foreach($s as $r)$overrides[(int)$r['price_list_id']]=(float)$r['markup_percentage'];}
 pageTop($p['id']?'Editar producto':'Nuevo producto',$a);?><div class="card"><div class="actions"><div style="flex:1"><h1><?=$p['id']?'Editar producto':'Nuevo producto'?></h1><p class="muted">Cada lista toma su porcentaje general. Podés cambiarlo acá solamente para este producto.</p></div><a class="btn light" href="?a=products">Volver</a></div><?php if(!$hasOverrides):?><div class="flash">Falta ejecutar la migración <b>2026_09_07_product_price_overrides.sql</b>.</div><?php endif;?><form method="post" action="?a=<?=$p['id']?'update_product':'save_product'?>"><input type="hidden" name="csrf" value="<?=e2($_SESSION['csrf'])?>"><?php if($p['id']):?><input type="hidden" name="product_id" value="<?=$p['id']?>"><?php endif;?><div class="grid"><p class="c4"><label>SKU / Referencia interna</label><input name="sku" value="<?=e2($p['sku'])?>" required></p><p class="c8"><label>Descripción</label><input name="description" value="<?=e2($p['description'])?>" required></p><p class="c4"><label>Costo USD</label><input id="cost" type="number" min="0" step=".01" name="cost_usd" value="<?=e2($p['cost_usd'])?>" required oninput="recalcPrices()"></p><p class="c4"><label>Categoría</label><select name="category_id"><option value="">Sin categoría</option><?php foreach($cats as $c):?><option value="<?=$c['id']?>" <?=$p['category_id']==$c['id']?'selected':''?>><?=e2($c['name'])?></option><?php endforeach;?></select></p><p class="c4"><label>Unidad</label><input name="unit" value="<?=e2($p['unit'])?>"></p><?php if(!$p['id']):?><p class="c4"><label>Stock inicial</label><input type="number" min="0" step=".001" name="initial_stock" value="0"></p><?php endif;?></div><h2>Precios por lista</h2><p class="muted">Si dejás el porcentaje igual al valor general, el producto seguirá automáticamente cualquier cambio futuro de esa lista. Si lo modificás, queda como porcentaje especial para este producto.</p><div class="grid"><?php foreach($lists as $l):$lid=(int)$l['id'];$def=(float)$l['markup_percentage'];$effective=$overrides[$lid]??$def;$special=array_key_exists($lid,$overrides);?><div class="c4 price-box"><label><?=e2($l['name'])?></label><div class="muted">General: +<?=e2($def)?>%</div><p><label>% para este producto</label><input class="markup-input" data-list="<?=$lid?>" data-default="<?=$def?>" type="number" min="0" step=".001" name="list_markup[<?=$lid?>]" value="<?=e2($effective)?>" oninput="recalcPrices()"></p><div>Precio: <span id="price-<?=$lid?>" class="price-value"><?=usd2(sale2((float)$p['cost_usd'],$effective))?></span></div><div id="mode-<?=$lid?>" class="<?=$special?'special':'muted'?>"><?=$special?'Porcentaje especial':'Usa porcentaje general'?></div></div><?php endforeach;?></div><p class="c12"><label style="font-weight:400"><input type="checkbox" name="track_stock" value="1" <?=$p['track_stock']?'checked':''?>> Controlar stock</label><label style="font-weight:400"><input type="checkbox" name="active" value="1" <?=$p['active']?'checked':''?>> Activo</label></p><button class="btn" <?=$hasOverrides?'':'disabled'?>>Guardar producto</button></form></div><script>function fmt(n){return 'US$ '+Number(n||0).toLocaleString('es-AR',{minimumFractionDigits:2,maximumFractionDigits:2})}function recalcPrices(){const cost=Number(document.getElementById('cost').value||0);document.querySelectorAll('.markup-input').forEach(i=>{const m=Number(i.value||0),d=Number(i.dataset.default||0),id=i.dataset.list;document.getElementById('price-'+id).textContent=fmt(cost*(1+m/100));const mode=document.getElementById('mode-'+id),special=Math.abs(m-d)>.0005;mode.textContent=special?'Porcentaje especial':'Usa porcentaje general';mode.className=special?'special':'muted';});}recalcPrices();</script><?php pageBottom();exit;
}
go2('index.php?a=products');